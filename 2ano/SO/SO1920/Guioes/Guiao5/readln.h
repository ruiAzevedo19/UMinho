#ifndef _READLN_H_
#define _READLN_H_

ssize_t readln(int fd, char* line, size_t size);

ssize_t readln_v2(int fd, char* line, size_t size);

ssize_t readln_v3(int fd, char* line, size_t size);

#endif

